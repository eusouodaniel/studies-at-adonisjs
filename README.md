# Studies at Adonisjs

## Start

```bash
adonis serve --dev (with auto refresh)
adonis serve (without auto refresh)
```

or manually clone the repo and then run `npm install`.

### Migrations

Run the following command to run startup migrations.

```js
adonis migration:run
```
